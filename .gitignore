node_modules
dist/
tmp/
*.js.map
*.d.ts.map
.env
.env.*
# Personal per-directory Memoree override (routing / opt-out). The committed
# `.memoree` is shared; `.memoree.local` is yours alone — never commit it.
.memoree.local
coverage/
jscpd-report/
bench/
.claude/
CLAUDE.md
RESULTS-fast-path-all-commands.md
PLAN-fast-path-all-commands.md
# Local-only test artifacts (npm pack tarballs, Bun lockfile, ad-hoc scripts)
*.tgz
bun.lock
deploy-to-cache.sh
.followups-pr97.md
.followups-pr98.md

# esbuild outputs — built via `npm run build` and validated by pack:check.
bundle/
harnesses/claude-code/bundle/
harnesses/codex/bundle/
evals/

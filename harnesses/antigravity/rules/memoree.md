# Memoree

`~/.memoree/memory` is a virtual filesystem. In Antigravity, read and write it with the Memoree MCP tools (`memoree_read`, `memoree_ls`, `memoree_grep`, `memoree_write`, `memoree_mv`, `memoree_rm`). Do not `cat` / `ls` / `grep` that path with `run_command` or `view_file`.

# Frozen pi experiment

This directory preserves the final pi integration snapshot for reference. It
is experimental and unsupported: it is excluded from TypeScript compilation,
runtime builds, CLI discovery, installation, automated tests, and support
claims. Do not modify or reactivate it without an explicit product decision.
